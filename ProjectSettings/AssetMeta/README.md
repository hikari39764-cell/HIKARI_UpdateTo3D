# Asset Meta

This folder stores project-level source asset metadata. Files are generated by
the asset database using the source asset path as a mirror, for example:

`ProjectSettings/AssetMeta/Assets/Textures/albedo.png.hikari.asset.json`

Cooked artifacts, import reports, thumbnails, and debug outputs belong in
`Library`.
