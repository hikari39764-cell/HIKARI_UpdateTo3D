#pragma once

namespace HIKARI {
    // Skeleton placeholder for next phase 3D model pipeline.
    class ModelManager {
    public:
        ModelManager() = default;
        ~ModelManager() = default;
    };
}
