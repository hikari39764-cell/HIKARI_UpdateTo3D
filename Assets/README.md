# Project Assets

Place source assets here. HIKARI keeps generated import metadata under
`ProjectSettings/AssetMeta` and cooked runtime outputs under `Library`, so this
folder should stay free of generated metadata sidecars.
